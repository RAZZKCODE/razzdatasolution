import React from 'react';
import { ArrowUpRight, Bot, Braces, ChartNoAxesCombined, Code2, Database, Globe2, Layers3, Workflow } from 'lucide-react';
import { PORTFOLIO_DATA } from '../data';
const icons = [Code2, Layers3, Globe2, Database, Braces, ChartNoAxesCombined, Workflow, Bot];
const Services = () => <section id="services" className="section-shell"><div className="section-heading"><div><span className="section-tag">What we build</span><h2 className="display-title">Systems that make <span className="text-gradient">work lighter.</span></h2></div><p>From the first interface to the last data pipeline, we build software around the way your business actually operates.</p></div><div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">{PORTFOLIO_DATA.services.map((service, index) => { const Icon = icons[index]; return <article key={service.title} className="service-card group"><div className="icon-box"><Icon /></div><div className="flex items-start justify-between gap-3"><h3>{service.title}</h3><ArrowUpRight className="w-4 h-4 text-muted group-hover:text-primary transition-colors" /></div><p>{service.details}</p><span className="service-link">Explore</span></article> })}</div></section>;
export default Services;
