import React from 'react';
import { ArrowUpRight, BarChart3, FileImage, Radar, Workflow } from 'lucide-react';
import { PORTFOLIO_DATA } from '../data';

const icons = [Radar, BarChart3, FileImage, BarChart3, Workflow];
const Projects = () => <section id="projects" className="section-shell border-t border-border/70"><div className="section-heading"><div><span className="section-tag">Selected work</span><h2 className="display-title">Ideas turned into <span className="text-gradient">systems.</span></h2></div><p>Representative products across data extraction, operational tooling, monitoring, and analytics.</p></div><div className="grid grid-cols-1 lg:grid-cols-2 gap-5">{PORTFOLIO_DATA.projects.map((project, index) => { const Icon = icons[index]; return <article key={project.name} className={`project-card ${index === 0 ? 'lg:col-span-2 featured-project' : ''}`}><div className="project-art"><Icon /><span>case_0{index + 1}</span><div className="art-lines" /></div><div className="project-copy"><div className="flex justify-between gap-5"><h3>{project.name}</h3><ArrowUpRight className="w-5 h-5 text-primary" /></div><p className="project-description">{project.description}</p><div className="case-flow"><div><small>Problem</small><span>{project.problem}</span></div><div><small>Solution</small><span>{project.solution}</span></div></div><div className="flex flex-wrap gap-2 mt-5">{project.tech.map((tech) => <span key={tech} className="data-pill">{tech}</span>)}</div><div className="project-features">{project.features.map((feature) => <span key={feature}>+ {feature}</span>)}</div></div></article> })}</div></section>;
export default Projects;
