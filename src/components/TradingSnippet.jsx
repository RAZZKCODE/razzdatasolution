import React, { useState } from 'react';
import { ArrowDown, ArrowUp, Database, Layers3, Monitor, Server, Sparkles, Workflow } from 'lucide-react';
import { PORTFOLIO_DATA } from '../data';
const icons = [Monitor, Layers3, Server, Database, Workflow, Sparkles, Monitor];
const TradingSnippet = () => { const [selected, setSelected] = useState(0); return <section id="process" className="section-shell architecture-section"><div className="section-heading"><div><span className="section-tag">How we work</span><h2 className="display-title">From idea to <span className="text-gradient">working system.</span></h2></div><p>A focused process that keeps the business problem visible through every technical decision.</p></div><div className="process-grid"><div className="process-list">{PORTFOLIO_DATA.process.map((step, index) => <button key={step} className={selected === index ? 'process-step active' : 'process-step'} onClick={() => setSelected(index)}><span>0{index + 1}</span><strong>{step}</strong><ArrowUp className="w-4 h-4" /></button>)}</div><div className="architecture-map"><div className="map-header"><span>system_architecture</span><span className="status-label"><i /> interactive model</span></div>{PORTFOLIO_DATA.architecture.map((layer, index) => { const Icon = icons[index]; return <React.Fragment key={layer}><div className={selected === index ? 'map-node selected' : 'map-node'}><Icon /><span>{layer}</span><small>{selected === index ? PORTFOLIO_DATA.process[Math.min(selected, PORTFOLIO_DATA.process.length - 1)] : 'connected layer'}</small></div>{index < PORTFOLIO_DATA.architecture.length - 1 && <ArrowDown className="map-arrow" />}</React.Fragment>; })}</div></div></section>; };
export default TradingSnippet;
